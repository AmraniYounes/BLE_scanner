/dts-v1/;

/ {
    buttons {
        compatible = "gpio-keys";
        boot_button: button_0 {
            gpios = <&gpio0 0 (GPIO_PULL_UP | GPIO_ACTIVE_LOW)>;
            label = "BOOT button";
        };
    };
    aliases {
        bt0 = &boot_button;
    };
};